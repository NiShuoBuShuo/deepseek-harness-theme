import { mkdir, readFile, writeFile } from 'node:fs/promises'
import { resolve } from 'node:path'

const root = resolve(import.meta.dirname, '..')
const css = await readFile(resolve(root, 'src/client/deep-whale.module.css'), 'utf8')
const background = (await readFile(resolve(root, 'assets/deep-whale-ocean.png'))).toString('base64')
const client = `// Generated by scripts/build.mjs; source lives in src/client/.\nconst css = ${JSON.stringify(css)};\nconst background = 'data:image/png;base64,${background}';\nconst OWNER = 'deep-whale';\nconst TITLE = 'Deep Whale · DeepSeek Harness';\nconst PROPERTIES = ['background-image','background-position','background-size','background-attachment','background-repeat'];\nfunction installCss(){ const id='@local/dsh-client-ui-skin-deep-whale/deep-whale.module.css'; if(document.querySelector('style[data-plugin-css="'+id+'"]')) return; const tag=document.createElement('style'); tag.dataset.pluginCss=id; tag.textContent=css; document.head.append(tag); }\nexport function apply(ctx){ installCss(); const body=document.body; const originalTitle=document.title; const previous=new Map(PROPERTIES.map(property=>[property,body.style.getPropertyValue(property)])); const meta=document.head.querySelector('meta[name="theme-color"]'); const previousThemeColor=meta?.content; body.dataset.dshDeepWhale=''; body.style.setProperty('background-image', 'url('+background+')'); body.style.setProperty('background-position','center top'); body.style.setProperty('background-size','cover'); body.style.setProperty('background-attachment','fixed'); body.style.setProperty('background-repeat','no-repeat'); if(meta) meta.content='#dff5ff'; document.title=TITLE; const ambience=document.createElement('div'); ambience.dataset.skinChrome='deep-whale-ambience'; ambience.dataset.skinOwner=OWNER; ambience.setAttribute('aria-hidden','true'); ambience.innerHTML='<i></i><i></i><i></i><i></i><i></i>'; body.prepend(ambience); const status=document.createElement('div'); status.dataset.skinChrome='deep-whale-status'; status.dataset.skinOwner=OWNER; status.setAttribute('aria-hidden','true'); status.innerHTML='<span class="deep-whale-pulse"></span><span>DEEP WHALE</span><small>ocean link stable</small>'; body.append(status); ctx.effect(()=>()=>{ delete body.dataset.dshDeepWhale; for(const [property,value] of previous) body.style.setProperty(property,value); document.querySelectorAll('[data-skin-owner="'+OWNER+'"]').forEach(element=>element.remove()); if(meta&&previousThemeColor!==undefined) meta.content=previousThemeColor; if(document.title===TITLE) document.title=originalTitle; }, 'ui-skin-deep-whale: ocean glass theme'); }\n`
await mkdir(resolve(root, 'lib'), { recursive: true })
await writeFile(resolve(root, 'lib/index.js'), '/** Host loader: browser-only skin. */\nexport function apply() {}\n')
await writeFile(resolve(root, 'lib/client.js'), client)
